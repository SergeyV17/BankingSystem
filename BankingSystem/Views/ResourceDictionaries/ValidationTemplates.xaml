<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ControlTemplate x:Key="validationFailed">
        <StackPanel>
            <Border BorderBrush="Red" BorderThickness="1">
                <AdornedElementPlaceholder/>
            </Border>
            <TextBlock Text="{Binding [0].ErrorContent}" Foreground="Red"/>
        </StackPanel>
    </ControlTemplate>

    <ControlTemplate x:Key="seriesrValidationFailed">
        <StackPanel>
            <Border BorderBrush="Red" BorderThickness="1"  Width="45">
                <AdornedElementPlaceholder/>
            </Border>
            <TextBlock Text="{Binding [0].ErrorContent}" Foreground="Red" Width="100" TextWrapping="Wrap" Margin="10 0 0 0"/>
        </StackPanel>
    </ControlTemplate>
</ResourceDictionary>